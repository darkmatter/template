groups:
  - name: agent-demo
    rules:
      - alert: OpsDemoUnavailable
        expr: up{job="agent-demo"} == 0
        for: 2m
        labels:
          severity: page
        annotations:
          summary: The agent-demo web service cannot be scraped.
