services:
  web-test:
    build:
      context: ../..
      dockerfile: ops/container/web/Dockerfile
    command:
      [
        "bun",
        "build",
        "--no-install",
        "--target=bun",
        "apps/web/src/server.ts",
        "--outfile=/tmp/ops-demo-check.js",
      ]
