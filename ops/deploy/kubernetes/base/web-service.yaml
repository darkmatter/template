apiVersion: v1
kind: Service
metadata:
  name: agent-demo-web
  labels:
    app.kubernetes.io/name: agent-demo
spec:
  selector:
    app.kubernetes.io/name: agent-demo
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 80
      targetPort: http
