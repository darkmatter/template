# Optional Kubernetes components

Components are reusable deployment additions. In a production repository this is where an ingress controller integration, a migration job, or a Prometheus `ServiceMonitor` would live. Environments opt into components explicitly rather than copying their manifests.
