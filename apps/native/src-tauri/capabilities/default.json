{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base permissions for the agent desktop window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
