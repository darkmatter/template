[package]
name = "agent-desktop"
version = "0.1.0"
description = "Desktop boundary for the Effect agent harness demo"
authors = ["Darkmatter"]
license = "MIT"
edition = "2021"
rust-version = "1.77.2"

[lib]
name = "agent_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.5.6", features = [] }

[dependencies]
tauri = { version = "2.10.3", features = [] }
